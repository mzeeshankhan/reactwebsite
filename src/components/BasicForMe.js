import { makeStyles } from '@material-ui/core';
import React from 'react';

const useStyle = makeStyles((theme)=>({
  

}));



const BasicForMe = () => {
  const classes = useStyle();
  return (
    <div>
      
    </div>
  )
}

export default BasicForMe
